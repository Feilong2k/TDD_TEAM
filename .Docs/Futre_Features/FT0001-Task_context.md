Allow attachment of task related context
Status: Completed

Once TDD tasks panel is in full function we can select the task we want to work on and tag messages so those messages are related to that task specifically

Notes:
Rather than having messages associated with tasks, we created a dashboard modal interactiing with the subtasks JSON file, so all questions and answers are located within the JSON file for the subtask