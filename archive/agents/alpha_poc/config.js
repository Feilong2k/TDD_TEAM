// Configuration for OpenAI AI SDK agent
// This file simply re-exports the configuration from config.local.js
// All configuration should be done in config.local.js

const config = require('./config.local');

module.exports = config;
